import { endServingToken } from "../api/actions";

export const useEndServing = (ip, tokenId, total_serving_time_display, counterId, sendMessage) => {
    const endServing = async () => {
        
    };

    return endServing;
};
